/** 
 @file  utility.h
 @brief ENet utility header
*/
#ifndef ENET_UTILITYH
#define ENET_UTILITYH

#define ENET_MAX(x, y) ((x) > (y) ? (x) : (y))
#define ENET_MIN(x, y) ((x) < (y) ? (x) : (y))

#endif /* ENET_UTILITYH */

